server:
  port: 8081
logging:
  file: application.log
  level:
    root: INFO
spring:
  application:
    name: tour_agency
  datasource:
    url: jdbc:mysql://localhost:3306/tour_agency?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: bananjikda1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
